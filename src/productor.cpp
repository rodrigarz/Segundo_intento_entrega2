#include "drone.h"
#include "buffer.h"

void productor(int id, BufferLimitado& buffer)
{
    Drone miDrone;
    miDrone.arrancar();
   // miDrone.subirHasta(10);
   // miDrone.bajarHasta(5);
    //miDrone.aterrizar();
}
